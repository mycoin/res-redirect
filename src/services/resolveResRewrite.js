const resolveResRewrite = (config, url) => {
  // return {
  //   redirectUrl: 'https://www.baidu.com',
  // }
  // for (let index = 0; index < config.length; index++) {
  //   const element = config[index]
  //   console.error(element)
  // }
}

export default resolveResRewrite
